on:
  - workflow_dispatch
  - pull_request
  - push

name: CI

jobs:

  lint:
    name: "lint and validate RAML spec"

    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v2

      - run: make lint
