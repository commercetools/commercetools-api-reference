#%RAML 1.0 DataType
(annotations.package): Callbacks
displayName: CartTypeEnum
type: string
description: |
  One of `sale`, `order`, `return`.
enum:
  - sale
  - order
  - return
(annotations.enumDescriptions):
  sale: |
    Cart to be ordered.
  order: |
    Cart that was ordered.
  return: |
    Cart that contains return items.
