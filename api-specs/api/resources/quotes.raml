type:
  baseDomain:
    resourceType: Quote
    resourceQueryType: QuotePagedQueryResponse
    resourceDraft: QuoteDraft
    whereExample: 'quoteState = "Accepted"'
    sortExample: createdAt asc
(updateable): Quote
(deleteable): Quote
(createable): QuoteDraft
description: A quote holds the negotiated offer.
get:
  displayName: Query Quotes
  description: Retrieves all Quotes in the Project.
  securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
  responses:
    200:
      body:
        application/json:
          example: !include ../examples/quotes.example.json
head:
  displayName: Check if Quote exists by Query Predicate
  description: Checks if one or more Quotes exist for the provided query predicate. Returns a `200 OK` status if any Quotes match the query predicate, or a [Not Found](/../api/errors#404-not-found) error otherwise.
  securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
  queryParameters:
    where?:
      type: QueryPredicate[]
post:
  displayName: Create Quote
  description: Creates a Quote in the Project.
  securedBy: [oauth_2_0: { scopes: ['manage_quotes:{projectKey}'] }]
  body:
    application/json:
      example: !include ../examples/quote-create.example.json
  responses:
    201:
      body:
        application/json:
          example: !include ../examples/quote.example.json
/key={key}:
  (methodName): withKey
  type:
    baseResource:
      uriParameterName: key
      resourceType: Quote
      resourceUpdateType: QuoteUpdate
  get:
    displayName: Get Quote by Key
    description: Retrieves a Quote with the provided `key`.
    securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
  head:
    displayName: Check if Quote exists by Key
    description: Checks if a Quote exists with the provided `key`. Returns a `200 OK` status if the Quote exists, or a [Not Found](/../api/errors#404-not-found) error otherwise.
    securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
  post:
    displayName: Update Quote by Key
    description: Updates a Quote in the Project using one or more [update actions](/../api/projects/quotes#update-actions).
    securedBy: [oauth_2_0: { scopes: ['manage_quotes:{projectKey}'] }]
    body:
      application/json:
        example: !include ../examples/quote-update.example.json
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
  delete:
    displayName: Delete Quote by Key
    description: Deletes a Quote in the Project.
    is:
      - dataErasure
    securedBy: [oauth_2_0: { scopes: ['manage_quotes:{projectKey}'] }]
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
/{ID}:
  (methodName): withId
  type:
    baseResource:
      uriParameterName: ID
      resourceType: Quote
      resourceUpdateType: QuoteUpdate
  get:
    displayName: Get Quote by ID
    description: Retrieves a Quote with the provided `id`.
    securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
  head:
    displayName: Check if Quote exists by ID
    description: Checks if a Quote exists with the provided `id`. Returns a `200 OK` status if the Quote exists, or a [Not Found](/../api/errors#404-not-found) error otherwise.
    securedBy: [oauth_2_0: { scopes: ['view_quotes:{projectKey}'] }]
  post:
    displayName: Update Quote by ID
    description: Updates a Quote in the Project using one or more [update actions](/../api/projects/quotes#update-actions).
    securedBy: [oauth_2_0: { scopes: ['manage_quotes:{projectKey}'] }]
    body:
      application/json:
        example: !include ../examples/quote-update.example.json
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
  delete:
    displayName: Delete Quote by ID
    description: Deletes a Quote in the Project.
    is:
      - dataErasure
    securedBy: [oauth_2_0: { scopes: ['manage_quotes:{projectKey}'] }]
    responses:
      200:
        body:
          application/json:
            example: !include ../examples/quote.example.json
