#%RAML 1.0 DataType
(package): Product
type: ProductUpdateAction
displayName: ProductAddAssetAction
discriminatorValue: addAsset
description: |
  Either `variantId` or `sku` is required.
example: !include ../../../examples/Product/ProductAddAssetAction.json
properties:
  variantId?:
    type: number
    format: int64
    description: |
      The `id` of the ProductVariant to update.
  sku?:
    type: string
    description: |
      The `sku` of the ProductVariant to update.
  staged?:
    type: boolean
    description: |
      Whether only staged `assets` are updated. If `false`, both current and staged `assets` are updated.
    default: true
  asset:
    type: AssetDraft
    description: |
      Value to append.
  position?:
    type: number
    format: int32
    description: |
      Position in `assets` where the Asset should be put. When specified, the value must be between `0` and the total number of Assets minus `1`.
