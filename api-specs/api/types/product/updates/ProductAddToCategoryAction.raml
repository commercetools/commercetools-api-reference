#%RAML 1.0 DataType
(package): Product
type: ProductUpdateAction
displayName: ProductAddToCategoryAction
discriminatorValue: addToCategory
example: !include ../../../examples/Product/ProductAddToCategoryAction.json
description: Produces the [ProductAddedToCategory](/projects/messages/product-catalog-messages#product-added-to-category) Message.
properties:
  category:
    type: CategoryResourceIdentifier
    description: |
      The Category to add.
  orderHint?:
    type: string
    pattern: ^0\.[0-9]*[1-9]$
    description: |
      A string representing a number between 0 and 1. Must start with `0.` and cannot end with `0`. If omitted, any existing value is removed.
  staged?:
    type: boolean
    description: |
      Whether only staged `categories` and `categoryOrderHints` are updated. If `false`, both current and staged `categories` and `categoryOrderHints` are updated.
    default: true
