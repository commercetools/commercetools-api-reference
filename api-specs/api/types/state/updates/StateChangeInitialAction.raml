#%RAML 1.0 DataType
(package): State
type: StateUpdateAction
displayName: StateChangeInitialAction
discriminatorValue: changeInitial
example: !include ../../../examples/State/StateChangeInitialAction.json
properties:
  initial:
    type: boolean
    description: |
      Whether to define the State as the initial State in a state machine and make it the first step in a workflow.
