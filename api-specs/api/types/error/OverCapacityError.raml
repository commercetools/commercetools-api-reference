#%RAML 1.0 DataType
(package): Error
type: ErrorObject
displayName: OverCapacityError
discriminatorValue: OverCapacity
description: |
  Returned when the service is having trouble handling the load.

  The client application should retry the request with exponential backoff up to a point where further delay is unacceptable.
properties:
  code:
    type: string
  message:
    type: string
    description: Plain text description of the error.
