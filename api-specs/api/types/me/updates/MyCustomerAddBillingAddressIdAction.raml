#%RAML 1.0 DataType
(package): Me
(docs-uri): https://docs.commercetools.com/api/projects/me-profile#add-billing-address-id
type: MyCustomerUpdateAction
displayName: MyCustomerAddBillingAddressIdAction
discriminatorValue: addBillingAddressId
example: !include ../../../examples/Customer/CustomerAddBillingAddressIdAction.json
description: |
  Adds an address from the `addresses` array to `billingAddressIds`. Either `addressId` or `addressKey` is required.
properties:
  addressId?:
    type: string
    description: |
      `id` of the [Address](ctp:api:type:Address) to become a billing address.
  addressKey?:
    type: string
    description: |
      `key` of the [Address](ctp:api:type:Address) to become a billing address.
