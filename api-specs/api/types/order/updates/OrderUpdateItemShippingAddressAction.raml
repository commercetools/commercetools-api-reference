#%RAML 1.0 DataType
(package): Order
example: !include ../../../examples/Order/OrderUpdateItemShippingAddressAction.json
type: OrderUpdateAction
displayName: OrderUpdateItemShippingAddressAction
discriminatorValue: updateItemShippingAddress
description: |
  Updates an address in `itemShippingAddresses` by keeping the Address `key`.
properties:
  address:
    type: AddressDraft
    description: |
      The new Address with the same `key` as the Address it will replace.
