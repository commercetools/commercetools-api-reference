#%RAML 1.0 DataType
(package): Cart
type: CartUpdateAction
displayName: CartUpdateItemShippingAddressAction
discriminatorValue: updateItemShippingAddress
example: !include ../../../examples/Cart/CartUpdateItemShippingAddressAction.json
description: |
  Updates an address in `itemShippingAddresses` by keeping the Address `key`.
properties:
  address:
    type: AddressDraft
    description: |
      The new Address with the same `key` as the Address it will replace.
