#%RAML 1.0 Trait

queryParameters:
  storeProjection:
    type: string
    required: false
    description: |
      `key` of an existing [Store](ctp:api:type:Store).
      If the Store defines multiple languages, they are used to [project the Product](ctp:api:type:ProductProjectionLocales).
