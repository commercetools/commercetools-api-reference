#%RAML 1.0 ResourceType

is: [right-header]
get:
  description: Returns information about the project locales setup and the environment in which the requested host acts in.
  responses:
    200:
      description: Locales and environment information.
      body:
        application/json:
          type: ProjectContext
