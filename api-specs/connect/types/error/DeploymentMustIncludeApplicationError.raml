#%RAML 1.0 DataType
(annotations.package): error
type: ErrorObject
displayName: DeploymentMustIncludeApplicationError
discriminatorValue: DeploymentMustIncludeApplication
description: |
  Returned when attempting to remove an application that belongs to the Deployment.
properties:
  code:
    type: string
  message:
    type: string
    description: |
      `"Deployment with id=$resourceId or key=$resourceKey must include application: $applicationName"`
