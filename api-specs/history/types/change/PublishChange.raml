#%RAML 1.0 DataType
(package): Change
type: Change
displayName: PublishChange
discriminatorValue: PublishChange
description: Change triggered by the [Publish](ctp:api:type:ProductPublishAction) update action.
properties:
  type:
    type: string
    enum:
      - PublishChange
  change:
    type: string
    enum:
      - publish
